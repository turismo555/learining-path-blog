<%= link_to "Edit", edit_blog_post_path(@blog_post) if user_signed_in? %>
<h1><%= @blog_post.title %></h1>
<%= @blog_post.body %>

<%= button_to "Delete", blog_post_path(@blog_post), method: :delete if user_signed_in? %>